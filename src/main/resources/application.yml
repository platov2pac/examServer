spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/exam
    username: postgres
    password: ROOT
    driver-class-name: org.postgresql.Driver
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.exams.dto